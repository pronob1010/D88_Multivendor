import FeaturedProductsList from './../../Data/featured_products';

const initialState = {
    products : FeaturedProductsList,
}

export const FeaturedProductReducer = (state = initialState, action) => {
    return state;
}