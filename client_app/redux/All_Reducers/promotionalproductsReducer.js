import PromotionalProductsList from './../../Data/promotional_products';

const initialState = {
    products : PromotionalProductsList,
}

export const PromotionalProductReducer = (state = initialState, action) => {
    return state;
}